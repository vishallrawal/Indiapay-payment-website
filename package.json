{
  "name": "aurapay-root",
  "version": "1.0.0",
  "description": "Root orchestrator for AuraPay fullstack application",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "start": "npx --yes concurrently \"npm start --prefix backend\" \"npm run dev --prefix frontend\""
  }
}
